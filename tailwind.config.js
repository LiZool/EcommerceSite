/** @type {import('tailwindcss').Config} */
module.exports = {
  mode: 'jit', // Make sure JIT mode is enabled  
  content: ["./src/**/*.{html,js,jsx,ts,tsx}"],
    theme: {
      extend: {
        
      },
    },
    plugins: [],
  }